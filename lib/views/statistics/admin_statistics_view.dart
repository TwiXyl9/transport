import 'package:flutter/material.dart';

class AdminStatisticsView extends StatelessWidget {
  const AdminStatisticsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
